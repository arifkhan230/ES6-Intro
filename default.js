// default---> if value is not provided , take this as a default.
function add(num1 = 12, num2 = 12) {
    const sum = num1 + num2;
    return sum;
}
const result = add();

function fullName(first, last) {
    const full = first + ' ' + last;
    return full;
}
const name = fullName('arif');
// console.log(name);

function numbers(numbs = []) {
   
}
function object (obj = {}){
    
}


